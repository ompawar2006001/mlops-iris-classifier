# mlops-iris-classifier — Version B

A sample ML project used to demonstrate Git-based version control
workflows in an MLOps context.

## Setup

```bash
pip install -r requirements.txt
python3 src/train.py

Press **⌘ + S**.

The README structure follows the lab manual; on your Mac we'll use `python3` rather than the Windows-style `python` command where necessary. :contentReference[oaicite:2]{index=2}

---

## Step 10 — Create the Python virtual environment

Now go back to your **VS Code Terminal**.

Run:

```bash
python3 -m venv .venv